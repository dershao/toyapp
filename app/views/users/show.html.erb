<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
	<strong>Sample of what this user has to offer:</strong>
	<% if (@user.microposts.length == 0) %>
		no microposts
	<% else %>
		<%= @user.microposts.first["content"] %>
	<% end %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
